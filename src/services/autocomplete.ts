import { Suggestion } from "../types/autocomplete";

// Placeholder for Phase 2 autocomplete
export async function getSuggestions(_input: string, _cwd: string): Promise<Suggestion[]> {
  return [];
}
